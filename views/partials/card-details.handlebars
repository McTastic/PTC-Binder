<div class="card">
  <div class="card-header" , data-id={{card.id}}>{{card.name}}
    -
    {{card.set.name}}</div>
  <div class="card-body">
    <img src={{card.images.small}} />
  </div>
  <div class="card-footer"></div>
</div>